allprojects {
    apply plugin: "eclipse"
    apply plugin: "idea"
    apply plugin: "java"
    
    repositories {
        mavenCentral()
    }

    ext {
        appName = 'sdsu-rocket-data'
    }

    tasks.withType(Test) {
        testLogging {
            events 'passed'
        }
    }
}

project(":server") {
    dependencies {
        compile project(":core");
    }
}

project(":client") {
    dependencies {
        compile project(":core");
    }
}

tasks.eclipse.doLast {
    delete ".project"
}
